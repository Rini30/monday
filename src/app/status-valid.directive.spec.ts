import { StatusValidDirective } from './status-valid.directive';

describe('StatusValidDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusValidDirective();
    expect(directive).toBeTruthy();
  });
});
