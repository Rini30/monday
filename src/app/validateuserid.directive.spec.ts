import { ValidateuseridDirective } from './validateuserid.directive';

describe('ValidateuseridDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateuseridDirective();
    expect(directive).toBeTruthy();
  });
});
